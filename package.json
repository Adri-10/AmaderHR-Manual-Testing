{
  "name": "amaderhr-manual-testing-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation starter for AmaderHR manual test assets",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:smoke": "playwright test tests/smoke.spec.ts",
    "test:list": "playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.9.2"
  }
}
